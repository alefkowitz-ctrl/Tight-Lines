// Full TripsTab - to be implemented in next session
// Carries over: trip planning, upcoming trips, weather fetch, gauges, client management

export default function TripsTab({ user, loc }) {
  return (
    <div className="info-box" style={{ marginTop: 20 }}>
      🗓 <strong>Trips</strong> coming soon in this rebuild.<br /><br />
      All your existing trip data is safe in Supabase.
    </div>
  )
}
